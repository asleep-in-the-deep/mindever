import SwiftUI

struct SettingsView: View {

    var body: some View {
        VStack {
            Text("Settings view")
        }
        .navigationBarHidden(true)
        .navigationTitle("")
    }
}

